[package]
name = "acvus-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
rust_decimal = "1.40.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
trait-variant = "0.1.2"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
