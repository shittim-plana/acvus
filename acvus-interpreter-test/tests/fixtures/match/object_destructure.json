{
  "template": "{{ { name, } = @user }}{{ name }} is here{{_}}no user{{/}}",
  "context": {
    "user": { "name": "alice", "age": 30, "email": "alice@example.com" }
  },
  "expected": "alice is here"
}
