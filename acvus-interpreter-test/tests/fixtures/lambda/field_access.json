{
  "template": "{{ x = $users | map(u -> u.name) }}{{ x | join(\", \") }}",
  "storage": {
    "users": [
      { "name": "alice", "age": 30 },
      { "name": "bob", "age": 25 }
    ]
  },
  "expected": "alice, bob"
}
