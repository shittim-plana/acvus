{
  "template": "{{ x = @items | iter | map(i -> i + 1) | collect }}{{ x | iter | map(i -> (i | to_string)) | collect | join(\", \") }}",
  "context": { "items": [1, 2, 3] },
  "expected": "2, 3, 4"
}
