{
  "template": "{{ x = @items | filter(|x| -> x != 0) | collect }}{{ x | map(|i| -> (i | to_string)) | collect | join(\", \") }}",
  "context": { "items": [0, 1, 2, 0, 3] },
  "expected": "1, 2, 3"
}
