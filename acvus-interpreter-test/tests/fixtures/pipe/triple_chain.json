{
  "template": "{{ x = @items | iter | filter(i -> i != 0) | map(i -> i + 1) | map(i -> (i | to_string)) | collect }}{{ x | join(\", \") }}",
  "context": { "items": [0, 1, 2, 3] },
  "expected": "2, 3, 4"
}
